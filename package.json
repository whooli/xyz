{
  "name": "newcleus-public-beta",
  "version": "0.0.1",
  "private": true,
  "description": "Whooli XYZ public beta dependency envelope",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:compression": "node -e \"const pc=require('picocolors'); console.log(pc.cyan('Whooli dependency lattice validated for platinum intake.'))\""
  },
  "dependencies": {
    "picocolors": "1.1.1"
  }
}
